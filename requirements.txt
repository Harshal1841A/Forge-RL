# FORGE-RL v2 — Unified Requirements

# ── Core ──────────────────────────────────────────────────────────
gymnasium>=0.29.1
numpy>=1.24.0
torch>=2.1.0
sentence-transformers>=2.6.0

# ── FORGE-MA Adversarial RL ───────────────────────────────────────
torch-geometric>=2.5.0
trl>=0.7.0
stix2>=3.0.0

# ── Server ────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
httpx>=0.27.0
python-dotenv>=1.0.0
duckduckgo-search>=5.0.0

# ── LLM ──────────────────────────────────────────────────────────
openai>=1.14.0
tenacity>=8.2.0

# ── Eval ─────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
openenv-core>=0.2.0

# ── Image (deepfake) ─────────────────────────────────────────────
Pillow>=10.0.0
torchvision>=0.16.0

# ── Knowledge Graph ───────────────────────────────────────────────
# graphify is a local tool output directory in this repo, not a pip package.